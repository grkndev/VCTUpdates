import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TeamsScreen() {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-white">Teams</Text>
      </View>
    </SafeAreaView>
  );
}
